<% if like = Like.find_by(user_id: current_user.id, post_id: post.id) %>
  <%= form_for [post, like] ,method: :delete, remote: true do |f| %>
    <%= hidden_field_tag :post_id, value: post.id %>
    <%= f.submit "いいね!済み" ,class:"jstestdelete_#{post.id}"%>
    <%= post.likes.count %>
    <%post.likes.each do |like| %>
      <%= like.user.username %>
    <% end %>
  <%#= link_to(post_like_path(post_id:post.id, id: post.like(current_user).id), {method: "delete"}, remote: true ) do %>
   <!--span class="active_heart"></span-->
  <% end %>
<% else %>
  <%= form_for [post, Like.new], method: :post, id: "form1", remote: true do |f| %>
    <%= f.submit "いいね!" ,id:"jstest_#{post.id}" %>
    <%= post.likes.count %>
    <%post.likes.each do |like| %>
      <%= like.user.username %>
    <% end %>
  <%#= link_to(post_likes_path(post_id: post.id), {method: "post"}, remote: true ) do %>
    <!--span class="inactive_heart"></span-->
  <% end %>
<% end %>
