Edit Comment
（コメントを編集するページ）

<%= form_for ( [@post, @comment] ) do |f| %>
  <p>
    <%= f.label :body, "コメント" %><br />
    <%= f.text_area :body, cols: 60, rows: 8 %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
